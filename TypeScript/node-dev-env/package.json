{
    "scripts": {
        "dev": "nodemon --watch src -e ts --exec ts-node src/index.ts",
        "built": "cross-env rm -rf ./dist && tsc"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "nodemon": "^3.0.1",
        "ts-node": "^10.9.1"
    },
    "dependencies": {
        "@types/node": "^20.10.1"
    }
}
